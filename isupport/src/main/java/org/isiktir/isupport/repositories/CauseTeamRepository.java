package org.isiktir.isupport.repositories;

import org.isiktir.isupport.domain.entities.CauseTeam;
import org.springframework.data.jpa.repository.JpaRepository;

public interface CauseTeamRepository extends JpaRepository<CauseTeam,String> {
}
